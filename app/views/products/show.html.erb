<h1>A product</h1>

<% @product.each do |x| %>
  <% if x['id'].to_s == params[:id] %>

    <img src= <%= x['image_url']%> >

    <p><%= x['name'] %></p>

    <p><%= x['tertiary_category'] %></p>

    <p><%= number_to_currency(x['price_in_cents'].to_f/100) %></p>
  <% end %>

<% end %>